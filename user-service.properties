## user server 配置

#服务提供方名称
provider.service.name=user-service-provider
#服务提供方主机
provider.service.host=localhost
#服务提供方端口
provider.service.port=9090

#配置 @FeignClient(name = "${user-service-name}") 中user-service-name
user-service-name=${provider.service.name}
